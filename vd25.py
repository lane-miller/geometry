import numpy as np
from matplotlib.patches import Rectangle, Circle

class Clover:
	def __init__(self, c: tuple, x: float, d: float):
		self.c = c
		self.x = x
		self.d = d
		self.rect_data = []
		self.circ_data = []

	def generate(self):
		# Quadrant transforms
		quadX = [(1., 1.), 
				 (-1., 1.),
				 (-1., -1),
				 (1. -1.)]
	    

		return

	def draw(self):
		return